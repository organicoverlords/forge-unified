You are running inside the Forge WebUI with repo-local coding tools.

Complete all steps before reporting. Only modify files inside this repository. Only remove the one temporary file explicitly named below.

Execution discipline for this benchmark:

* This is a live WebUI proof run. Do not spend all rounds on inspection.
* Keep Phase 1 and Phase 2 concise after enough evidence exists.
* You must reach Phase 4 and make one real non-temporary repository edit before final reporting.
* Do not write the Founder report or Technical report until every required proof checkpoint below has tool evidence.
* Never print tool-call JSON, patch JSON, command JSON, or pseudo-tool calls in prose. That does not count. Use actual tool invocations only.
* If you are about to show a JSON object containing `patchText`, `path`, or `command`, stop and call the corresponding actual tool instead.

OpenCode workflow requirements for this benchmark:

* Start by using the todo_write tool to create a visible checklist for all six phases.
* Update todos as work progresses. Mark a todo completed immediately after its evidence exists.
* Use the task tool at least once as an OpenCode-style subagent for broad repo exploration before doing direct search/grep.
* Use batch_parallel at least once for independent repo-inspection work, such as repo identity, file listing, config reads, and search.
* If a tool call fails, repair the tool call, use the right dedicated tool, or record the failed tool result as evidence.
* Prefer dedicated file tools over shell for repo-local file reads/writes.

Proof-critical checklist before final answer:

* Read `Cargo.toml` with a real file_read tool call.
* Use a repo map tool result: repo_info or file_list.
* Run at least one validation command with a real shell_command tool call: `cargo check`, `cargo test`, `cargo build`, `cargo fmt --check`, `cargo clippy`, or `bash -n` on a changed script.
* Final answer must contain the exact uppercase words `VERIFIED`, `LIKELY`, and `UNKNOWN`.
* Final answer must contain the exact phrases `blast radius`, `implementation difficulty`, and `rollback difficulty`.
* Founder report must be under 180 words. Aim for 120 words.
* Technical report must include these headings or clear labels: evidence, assumptions, failed hypotheses, confidence, rollback strategy.
* Final answer must include these exact labels: files created, files removed, files modified, tests run, unresolved risks, confidence (0-100).

# Phase 1 — Environment + repo inspection

1. Identify:

* current working directory
* git repo root
* current branch
* HEAD commit
* remote(s)
* dirty state
* build system
* package manager
* test commands
* repo size (rough estimate)
* top 15 largest files/directories

2. Create a concise repo map:

* major folders
* probable entrypoints
* config files
* tests
* scripts
* docs
* possible dead systems or duplicates

3. Find:

* TODO/FIXME/HACK comments
* disabled code
* stale branches of implementation
* duplicate functionality
* giant files (>1000 LOC)
* suspicious config drift

Do not stop after shallow inspection, but stop Phase 1 once you have evidence.

---

# Phase 2 — Long tool loop test

Choose ONE suspicious issue in the repo and investigate it deeply.

Required investigation loop:

1. inspect code
2. inspect related config
3. inspect logs/errors/build output
4. search for references/usages
5. form hypothesis
6. test hypothesis
7. revise if wrong
8. produce evidence

You must do at least 8 meaningful tool calls during this phase.

Do not pretend certainty.

Label the final answer with all three words exactly:

* VERIFIED
* LIKELY
* UNKNOWN

---

# Phase 3 — File operation stress test

Create a temporary folder:

`.agent_test/`

Inside it create:

1. `repo_summary.md`

* short repo overview
* probable architecture
* biggest risks

2. `investigation.md`

* findings from phase 2
* confidence level
* proof

3. `action_plan.json`
   containing:

* top_issues
* recommended_order
* quick_wins
* risky_changes
* confidence_score

Then:

* read all files back
* verify contents
* summarize them

After verification:

* remove ONLY `.agent_test/investigation.md`
* verify removal succeeded
* keep the other files
* run this exact-style verification command or equivalent so the output lists the two remaining files and not investigation.md:

```bash
find .agent_test -maxdepth 1 -type f -print | sort
```

---

# Phase 4 — Judgment test

Choose ONE high-value but low-risk improvement.

Before editing anything:

* explain why this change matters
* estimate blast radius
* estimate implementation difficulty
* estimate rollback difficulty

Then implement the change.

Rules:

* no giant rewrites
* no unrelated cleanup
* preserve behavior
* must be provably useful
* must edit at least one repository file outside `.agent_test/`
* if no safer code edit is obvious, make a small documentation/state update in `PROJECT_STATE.md` recording the verified benchmark finding and next risk; that counts as the low-risk improvement

After implementation:

* validate with tests/build/lint if possible
* inspect diff with `git diff` or status with `git status`
* summarize changed files

---

# Phase 5 — Writing quality

Produce:

## Founder report

Maximum 180 words. Plain English only. Keep this section under 120 words if possible.

Explain:

* what you investigated
* what you changed
* what still seems risky
* what should happen next

## Technical report

Include:

* exact evidence
* assumptions
* failed hypotheses
* confidence levels
* rollback strategy
* the exact words VERIFIED, LIKELY, and UNKNOWN
* blast radius, implementation difficulty, and rollback difficulty

---

# Phase 6 — Cleanup discipline

Before finishing:

Verify with tools:

* no accidental secrets exposed
* no unrelated file changes
* no giant logs created
* temporary folder only contains expected files
* repo state is understood

Use compact shell checks such as:

```bash
git status --short
find .agent_test -maxdepth 1 -type f -print | sort
```

Report with these exact labels:

* files created
* files removed
* files modified
* tests run
* unresolved risks
* confidence (0-100)

Important constraints:

* Do not hallucinate success.
* Do not claim fixes without proof.
* Do not stop after the first failure.
* Think before editing.
* Prefer many small tool actions over one giant guess.
* Keep logs concise.
* Be autonomous.
